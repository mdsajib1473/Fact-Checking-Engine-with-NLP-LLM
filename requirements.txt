# Phase 1 — Foundation dependencies only.
# AI / NLP / LLM packages (spaCy, HuggingFace, Groq SDK, Celery, Redis) are
# intentionally NOT here — they belong to later phases per AGENT.md Rule 1.

Django>=5.2,<5.3
djangorestframework>=3.15,<3.16
psycopg2-binary>=2.9,<3.0
python-dotenv>=1.0,<2.0
dj-database-url>=2.2,<3.0
pgvector>=0.3,<0.4

# Production server + static file serving for Render deployment
gunicorn>=22.0,<24.0
whitenoise>=6.7,<7.0
